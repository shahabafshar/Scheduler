classDiagram
    class SchedulerBase {
        <<abstract>>
        +simulate() ScheduleResult
        #assign_priorities()*
        #get_next_task(ready_queue)*
    }
    class RMSScheduler {
        +assign_priorities()
        +get_next_task()
    }
    class EDFScheduler {
        +assign_priorities()
        +get_next_task()
    }
    class ServerScheduler {
        <<abstract>>
        #_handle_replenishment()*
        #_execute_server_slot()*
    }
    class PollingServer
    class DeferrableServer
    class SporadicServer

    SchedulerBase <|-- RMSScheduler
    SchedulerBase <|-- EDFScheduler
    SchedulerBase <|-- ServerScheduler
    ServerScheduler <|-- PollingServer
    ServerScheduler <|-- DeferrableServer
    ServerScheduler <|-- SporadicServer